{
  "name": "@project-aegis/root",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts"
  },
  "workspaces": [
    "agents/*",
    "packages/*"
  ],
  "dependencies": {
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "eslint": "^8.53.0",
    "eslint-config-prettier": "^9.0.0",
    "prettier": "3.1.0"
  }
}
